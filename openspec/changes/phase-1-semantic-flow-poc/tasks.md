## 0. M0 正式立项与基线

- [x] 0.1 `T0.1` 初始化 Git、MIT 许可证、公开 README 和忽略规则，确认第三方样例、数据库、缓存、依赖和构建产物不入库
- [x] 0.2 `T0.2` 创建 GitHub public repository，推送基线并从 GitHub 核对可见性和默认分支
- [x] 0.3 `T0.3` 严格校验一期 OpenSpec，检查 `P1-*` 需求与 `docs/phase-1/` 的对应关系
- [x] 0.4 `T0.4` 运行旧原型构建作为对照基线，并记录它不代表已批准产品行为

## 1. M1 模型契约与确定性校验

- [x] 1.1 `T1.1` 先写并运行失败测试，覆盖缺 band、重复 ID、悬空边、无证据对象、越界路径和无效行号
- [x] 1.2 `T1.2` 用 Node 标准库实现最小 EvidencePack/SemanticModel 校验器，使结构负例失败、合法 fixture 通过
- [x] 1.3 `T1.3` 先写失败测试再实现注册/登录真值门禁：成功、失败、注册事件、非空数据与事件标签
- [x] 1.4 `T1.4` 固定样例 manifest、schemaVersion、仓库提交、入口符号、分析路径和中文解释语言

## 2. M2 真实证据包

- [x] 2.1 `T2.1` 提供幂等的样例获取与 CodeGraph 建图命令，确保第三方源码和 `.codegraph` 保持未跟踪
- [x] 2.2 `T2.2` 先写固定 SQLite fixture 的失败测试，再实现相关实体、关系、调用位置和 provider metadata 的有界查询
- [x] 2.3 `T2.3` 先写路径/行号失败测试，再实现样例根目录内的精确源码片段读取
- [x] 2.4 `T2.4` 原子写入 `evidence.json` 与统计摘要，失败时保留上次有效产物
- [x] 2.5 `T2.5` 对入口、嵌套调用、条件、依赖注入、返回和事件完成源码抽查并记录结果

## 3. M3 受约束语义建图

- [x] 3.1 `T3.1` 编写 AI authoring prompt，限定只读 evidence、严格 schema、证据引用、禁止读取旧硬编码模型和禁止编造
- [x] 3.2 `T3.2` 使用当前 AI 代理生成原始 `model.json`，记录模型/提示版本和执行次数
- [x] 3.3 `T3.3` 运行结构与真值校验，保留每次拒绝原因并且不自动放宽规则
- [x] 3.4 `T3.4` 记录所有人工文案纠正和拓扑修改；出现程序员拓扑重建时触发 STOP/REVISE
- [x] 3.5 `T3.5` 只把验证通过的 model/evidence 子集原子发布到浏览器 generated 目录

## 4. M4 最小理解界面

- [x] 4.1 `T4.1` 按 RED/GREEN 实现相机纯函数：scale clamp、滞回 band、指针锚点缩放和中键平移状态
- [x] 4.2 `T4.2` 先写当前 band 渲染测试，再实现原生 SVG 流程投影，一次只挂载 Z0/Z1/Z2 之一
- [x] 4.3 `T4.3` 先写可见祖先测试，再实现跨 band selection 映射和稳定父区域
- [x] 4.4 `T4.4` 先写碰撞与固定状态测试，再实现近指针 hover、单击 pin、外部点击/Escape 取消
- [x] 4.5 `T4.5` 先写状态保持浏览器测试，再实现源码证据抽屉的打开、关闭、路径、行号和文本片段
- [x] 4.6 `T4.6` 建立 feature/flow 独立 URL，并用 BroadcastChannel 同步 semantic ID、保留各自相机

## 5. M5 产品可行性评审

- [x] 5.1 `T5.1` 运行全量单元、契约、集成、浏览器和 Vite 构建验证，记录命令、提交和结果
- [x] 5.2 `T5.2` 按 `MT-TRUTH-*` 核对所有功能主张、条件、结果、传递和源码范围
- [ ] 5.3 `T5.3` 让目标用户在不先读源码时完成任务 A-E，原样记录回答和误解
- [ ] 5.4 `T5.4` 将语义缩放与旧点击下钻原型比较并记录四项 easier/equal/harder
- [ ] 5.5 `T5.5` 按既定阈值记录 GO/REVISE/STOP；GO 时只授权第二种图语法和模拟区域加载验证
